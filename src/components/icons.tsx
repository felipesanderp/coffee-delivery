import { ShoppingCart, MapPin, Timer, Package, Coffee } from 'lucide-react'

export const Icons = {
  shoppingCart: ShoppingCart,
  mapPin: MapPin,
  timer: Timer,
  package: Package,
  coffee: Coffee,
}
